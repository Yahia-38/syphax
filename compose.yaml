services:
  mongodb:
    image: mongo:7.0
    container_name: syphax-mongodb
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - syphax_mongodb_data:/data/db

volumes:
  syphax_mongodb_data:
